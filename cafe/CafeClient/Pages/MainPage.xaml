<Page x:Class="CafeClient.Pages.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      d:DesignHeight="800" d:DesignWidth="1200"
      Background="#FAFAFA"
      Title="Главное меню"
      Loaded="Page_Loaded">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Row="0" Grid.ColumnSpan="3" Height="60" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0" VerticalContentAlignment="Center" FontSize="17" Padding="10" Margin="5"
                     materialDesign:HintAssist.Hint="Поиск по меню..." Style="{StaticResource MaterialDesignOutlinedTextBox}" TextChanged="TextBox_TextChanged"/>
            
            <Button Grid.Column="1" Height="40" Margin="5" Content="Создать заказ" Click="CreateOrder_Click"/>
            <Button Grid.Column="2" Height="40" Margin="5" Content="Обновить" Click="RefreshButton_Click"/>
            <Button x:Name="ButtonMenu" Grid.Column="3" Height="40" Margin="5" Content="Управление меню" Click="AddMenu_Click"/>
            <Button x:Name="ButtonUser" Grid.Column="4" Height="40" Margin="5" Content="Пользователи" Click="Button_Click"/>
            <Button Grid.Column="5" Height="40" Margin="5" Content="Экран кухни" Click="OpenKitchen_Click"/>
            <Button Grid.Column="6" Height="40" Margin="5" Content="Выйти" Click="LogoutButton_Click" Background="#D32F2F" BorderBrush="#D32F2F"/>
        </Grid> 

        <Border Grid.Row="1" Grid.Column="0" Background="White" BorderBrush="#DDDDDD" BorderThickness="1" Padding="10">

            <TabControl Style="{StaticResource MaterialDesignTabControl}">

                <TabItem Header="Активные" FontSize="16" FontWeight="SemiBold">
                    <ListView x:Name="OrdersListView"
                              BorderThickness="0"
                              Background="Transparent"
                              MouseDoubleClick="OrdersListView_MouseDoubleClick"
                              ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                              ScrollViewer.VerticalScrollBarVisibility="Auto">

                        <ListView.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ListView.ItemsPanel>

                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <materialDesign:Card Padding="10" UniformCornerRadius="6">
                                    <StackPanel>
                                        <TextBlock Margin="5" Text="{Binding TableNumber, StringFormat=Столик №{0}}" FontWeight="Bold" FontSize="25"/>
                                        <TextBlock Margin="5" Text="{Binding UserName}" FontSize="16" FontWeight="SemiBold" Foreground="DimGray"/>

                                        <TextBlock Text="{Binding Status}" FontSize="18" Foreground="#1976D2"/>

                                        <TextBlock Margin="0,5,0,0">
                                            <Run Text="Добавлен:" Foreground="Green"/>
                                            <Run Text="{Binding CreatedAt, StringFormat=HH:mm}" Foreground="Green" FontWeight="Bold"/>
                                        </TextBlock>

                                        <TextBlock Text="{Binding TotalAmount, StringFormat={}{0} ₽}" FontWeight="Bold" FontSize="16" Margin="0,10,0,0"/>
                                    </StackPanel>
                                </materialDesign:Card>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </TabItem>

                <TabItem Header="Архив" FontSize="16">
                    <ListView x:Name="HistoryListView"
                              BorderThickness="0"
                              Background="Transparent"
                              MouseDoubleClick="HistoryListView_MouseDoubleClick"
                              ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                              ScrollViewer.VerticalScrollBarVisibility="Auto">

                        <ListView.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ListView.ItemsPanel>

                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <materialDesign:Card Margin="5" Padding="15" UniformCornerRadius="6" Background="#F5F5F5" Width="185">
                                    <StackPanel>
                                        <TextBlock Margin="5" Text="{Binding TableNumber, StringFormat=Столик №{0}}" FontWeight="Bold" FontSize="23" Foreground="Gray"/>
                                        <TextBlock Margin="5" Text="{Binding UserName}" FontSize="16" Foreground="Gray"/>
                                        <TextBlock Margin="5" Text="{Binding Status}" FontSize="18" Foreground="Green" FontWeight="Bold"/>
                                        <TextBlock Margin="5" Text="{Binding CreatedAt, StringFormat=dd.MM.yyyy HH:mm}" Foreground="Gray"/>
                                        <TextBlock Margin="5" Text="{Binding TotalAmount, StringFormat={}{0} ₽}" FontWeight="Bold" FontSize="16" Foreground="Gray"/>
                                    </StackPanel>
                                </materialDesign:Card>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </TabItem>

            </TabControl>
        </Border>

        <Border Grid.Row="1" Grid.Column="2" Background="White" BorderBrush="#DDDDDD" BorderThickness="1" Margin="10,0">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top" Text="Категории" HorizontalAlignment="Center" FontSize="30" FontWeight="SemiBold" Margin="5"/>
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <WrapPanel Margin="5" HorizontalAlignment="Center">
                        <Button Height="60" Width="150" Margin="5" FontSize="18" Content="Пицца" Tag="Пицца" Click="CategoryButton_Click"/>
                        <Button Height="60" Width="150" Margin="5" FontSize="18" Content="Напитки" Tag="Напитки" Click="CategoryButton_Click"/>
                        <Button Height="60" Width="150" Margin="5" FontSize="18" Content="Десерты" Tag="Десерты" Click="CategoryButton_Click"/>
                        <Button Height="60" Width="150" Margin="5" FontSize="18" Content="Супы" Tag="Супы" Click="CategoryButton_Click"/>
                        <Button Height="60" Width="150" Margin="5" FontSize="18" Content="Салаты" Tag="Салаты" Click="CategoryButton_Click"/>
                        <Button Height="60" Width="150" Margin="5" FontSize="18" Content="Горячее" Tag="Горячее" Click="CategoryButton_Click"/>
                        <Button Height="60" Width="150" Margin="5" FontSize="18" Content="Закуски" Tag="Закуски" Click="CategoryButton_Click"/>
                    </WrapPanel>

                </ScrollViewer>
            </DockPanel>
        </Border>

        <Border Grid.Row="1" Grid.Column="1" Background="White" BorderBrush="#DDDDDD" BorderThickness="1" Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <TextBlock Grid.Row="1" Text="Меню" FontSize="30" FontWeight="SemiBold" Margin="0,0,0,10"/>
                <ListView Grid.Row="2" x:Name="MenuListView" BorderThickness="0" 
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled" 
                  ScrollViewer.VerticalScrollBarVisibility="Disabled" 
                  HorizontalContentAlignment="Stretch">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <materialDesign:Card Margin="0,5" UniformCornerRadius="8" Padding="10">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <StackPanel Grid.Row="0">
                                        <TextBlock Text="{Binding Name}" FontSize="20" FontWeight="Bold" TextWrapping="Wrap"/>
                                        <TextBlock Text="{Binding Category}" FontSize="16" Foreground="Gray" TextWrapping="Wrap"/>
                                    </StackPanel>

                                    <TextBlock Grid.Row="1" Text="{Binding Description}" FontSize="16" 
                                       TextWrapping="Wrap" Margin="0,10" Foreground="#444444"/>

                                    <Grid Grid.Row="2" Margin="0,5,0,0">
                                        <TextBlock Text="{Binding Price, StringFormat={}{0} ₽}" FontWeight="Bold" Foreground="Green" FontSize="20" VerticalAlignment="Center"/>
                                        <Button Style="{StaticResource MaterialDesignFloatingActionMiniButton}" 
                                        HorizontalAlignment="Right" Width="40" Height="40"
                                        Click="AddToCart_Click">
                                            <materialDesign:PackIcon Kind="Plus"/>
                                        </Button>
                                    </Grid>
                                </Grid>
                            </materialDesign:Card>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Border>

    </Grid>
</Page>