<Page x:Class="CafeClient.Pages.KitchenPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      d:DesignHeight="1080" d:DesignWidth="1920"
      Title="Кухонный Экран"
      Background="White"
      Loaded="Page_Loaded" Unloaded="Page_Unloaded">

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Menu VerticalAlignment="Top" HorizontalAlignment="Stretch" Background="Black" Foreground="White">
                <MenuItem Header="Навигация">
                    <MenuItem Header="Назад" Click="BackMenuItem_Click" Foreground="Black"/>
                    <MenuItem Header="Выход" Click="ExitMenuItem_Click" Foreground="Black"/>
                </MenuItem>

            </Menu>

        </Grid>

        <Grid Grid.Row="1" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="ЗАКАЗЫ В РАБОТЕ" Foreground="White" FontSize="32" FontWeight="Bold" HorizontalAlignment="Left" VerticalAlignment="Top" Grid.ColumnSpan="2"/>
            <TextBlock x:Name="ClockTextBlock" Foreground="Black" FontSize="32" FontWeight="Bold" HorizontalAlignment="Left" Grid.Column="1" Margin="1824,0,0,0"/>
        </Grid>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Hidden" PanningMode="VerticalOnly">

            <ItemsControl x:Name="KitchenListView">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" HorizontalAlignment="Left"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <materialDesign:Card Width="320" Margin="15" UniformCornerRadius="8" Background="White">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <StackPanel Grid.Row="0">
                                    <Border Background="#424242" Padding="10">
                                        <DockPanel>
                                            <TextBlock Text="{Binding TableNumber, StringFormat=Стол #{0}}" Foreground="White" FontSize="24" FontWeight="Bold" DockPanel.Dock="Left"/>
                                            <TextBlock Text="{Binding CreatedAt, StringFormat=HH:mm}" Foreground="White" FontSize="24" FontWeight="Bold" DockPanel.Dock="Right" HorizontalAlignment="Right"/>
                                        </DockPanel>
                                    </Border>

                                    <ItemsControl ItemsSource="{Binding Items}" Margin="10">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Grid Margin="0,5">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                    </Grid.ColumnDefinitions>

                                                    <Border Grid.Column="0" Background="#D32F2F" CornerRadius="4" Width="35" Height="35" Margin="0,0,10,0">
                                                        <TextBlock Text="{Binding Quantity}" 
                                                            Foreground="White" 
                                                            FontWeight="Bold" 
                                                            FontSize="20" 
                                                            HorizontalAlignment="Center" 
                                                            VerticalAlignment="Center"/>
                                                    </Border>

                                                    <TextBlock Grid.Column="1" 
                                                       Text="{Binding MenuItemName}" 
                                                       FontSize="20" 
                                                       FontWeight="SemiBold" 
                                                       TextWrapping="Wrap" 
                                                       VerticalAlignment="Center"/>
                                                </Grid>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>

                                <Border Grid.Row="1" Background="#EEEEEE" Padding="8">
                                    <TextBlock Text="{Binding Status}" 
                                           FontSize="18" 
                                           FontWeight="Bold" 
                                           HorizontalAlignment="Center" 
                                           Foreground="Black"/>
                                </Border>
                            </Grid>
                        </materialDesign:Card>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Page>